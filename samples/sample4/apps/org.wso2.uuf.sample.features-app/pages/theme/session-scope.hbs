{{layout "org.wso2.uuf.base.main"}}
{{title "Session Theme Sample | " @app.config.appName}}
{{#fillZone "content"}}
    <h3>Session Scoped Theme Sample.</h3>
    <hr />
    <p>You can set an session-scope theme using your executable.
       In your JS file;</p>
    <pre>
        var session = getSession();
        if (!session) {
            session = createSession("admin");
        }
        session.setThemeName("org.wso2.uuf.theme.green");
    </pre>
    <br />
    <h3>Notes:</h3>
    <ul>
        <li>
            Changing theme through the server-side Js will be affected from the next HTTP request.
            To view changed theme; please refresh this page.
        </li>
        <li>
            Setting <code>session-scope</code> theme will affect only current session.
        </li>
    </ul>
{{/fillZone}}