<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    {{css "lib/bootstrap-v3.3.5/bootstrap.min.css"}}
    {{headJs "lib/jquery-v2.2.3/jquery.min.js"}}
    {{headJs "lib/bootstrap-v3.3.5/bootstrap.min.js"}}
    {{css "css/theme.css" }}
    <title>WSO2 Template</title>

    {{placeholder 'headJs'}}
    {{placeholder 'css'}}
</head>
<body>
<header class="header header-default">
    <div class="container-fluid">
        <div class="pull-left brand float-remove-xs text-center-xs">
            <a href="{{@app.context}}/">
                <img src="{{public "images/logo.png"}}" class="logo" />
                <h1>Unified UI Template App</h1>
            </a>
        </div>
        <div class="pull-right auth float-remove-xs text-center-xs">
            <ul class="nav navbar-nav navbar-right">
                <li><a href="{{@app.context}}/basicauth/login">Login</a></li>
            </ul>
        </div>
    </div>
</header>
<nav class="navbar navbar-default">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <!------start of menu----->
            {{#menu "main"}}
                {{~#if this~}}
                    <ul class="nav navbar-nav">
                        {{~#each this}}
                            <!------end menu item----->
                            {{~#if link}}
                                <li><a href="{{@app.context}}/{{link}}"><i class="{{icon}}"></i>{{text}}</a></li>
                            {{~else}}
                            <!------sub menu item----->
                                {{~#each this}}
                                    <li class="dropdown">
                                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button"
                                           aria-haspopup="true" aria-expanded="false">{{@key}} <span class="caret">
                                        </span></a>
                                        <ul class="dropdown-menu">
                                            {{~#each this}}
                                                {{~#if link}}
                                                    <li>
                                                        <a href="{{@app.context}}/{{link}}">
                                                            <i class="{{icon}}"></i> {{text}}
                                                        </a>
                                                    </li>
                                                {{~/if}}
                                            {{~/each}}
                                        </ul>
                                    </li>
                                {{~/each}}
                            {{~/if}}
                        {{~/each}}
                    </ul>
                {{~/if}}
            {{/menu}}
            <!------end of menu----->
        </div>
    </div>
</nav>
<div class="page-content-wrapper">
    <div class="container-fluid body-wrapper">
        {{defineZone "content"}}
    </div>
</div>
{{placeholder "js"}}
</body>
</html>
